# Drawing bezier curves using the de Casteljau algorithm

Press `space` to place a new point to a random location

Press `backspace` to remove the last point

You can move points around using your mouse